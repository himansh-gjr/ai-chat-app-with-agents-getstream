Ada / Babbage / Curie / Davinci → GPT-3 family → text completion API

You gave them text, and they predicted what text should come next.

Example:

The capital of France is

→ Paris.

The key distinction:

GPT-3 Davinci → completion model
ChatGPT → instruction/chat model built on newer GPT models
Modern GPT models → much better at following instructions, reasoning, tool use, structured output, etc.